# 한국투자증권 Open API
# KIS Developers 포털에서 발급: https://apiportal.koreainvestment.com
# 주의: 순위분석 API는 모의투자에서 지원되지 않으므로 실전투자 계정 필요
KIS_APP_KEY=your_kis_app_key
KIS_APP_SECRET=your_kis_app_secret
KIS_ACCOUNT_NO=12345678-01

# 텔레그램 봇 설정
# @BotFather에서 봇 생성 후 토큰 발급
TELEGRAM_TOKEN=1234567890:ABCdefGHIjklMNOpqrSTUvwxYZ
# 채널 또는 그룹 ID (숫자 형식)
CHAT_ID=-1001234567890

# 네이버 검색 API (뉴스 수집용)
# https://developers.naver.com 에서 애플리케이션 등록 후 발급
# 일일 25,000회 무료
NAVER_CLIENT_ID=your_naver_client_id
NAVER_CLIENT_SECRET=your_naver_client_secret

# Supabase 설정 (API 키 + 토큰 중앙 관리, 강력 권장)
# 로컬과 GitHub Actions 간 토큰을 공유하여 1일 1회 발급 제한 문제를 방지합니다.
#
# 설정 방법:
# 1. Supabase 프로젝트 생성: https://supabase.com
# 2. api_credentials 테이블 생성 (service_name, credential_type, credential_value 컬럼)
# 3. KIS API 키 저장: service_name='kis', credential_type='app_key'/'app_secret'
#
# 동작 방식:
# - 토큰 발급 시 Supabase에 자동 저장
# - 다른 환경에서 실행 시 Supabase에서 토큰 로드
# - 로컬/GitHub Actions 모두 같은 토큰 공유
#
# 미설정 시 로컬 파일 캐시만 사용 (환경 간 토큰 충돌 가능)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key
